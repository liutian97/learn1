// 1. 导入需要的包
// 注意：导入的名称，就是装包时候的名称
const moment = require('moment');
const dt = moment().format('YYYY-MM-DD hh:mm:ss'); // 调用moment函数，使用format()方法进行格式化
console.log(dt);


























// // 注意：导入的名称，就是装包时候的名称
// const moment = require('moment')

// const dt = moment().format('YYYY-MM-DD HH:mm:ss')
// console.log(dt)

// moment()   调用moment函数